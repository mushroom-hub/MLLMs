import { createPopper as defaultCreatePopper } from '@popperjs/core';
import type { Instance as PopperInstance, Options as PopperOptions, VirtualElement } from '@popperjs/core';
import type { Styles } from '../common';
type Options = Partial<PopperOptions & {
    createPopper: typeof defaultCreatePopper;
}>;
type Attributes = {
    [key: string]: {
        [key: string]: string;
    };
};
export interface InnerPopperInstance extends PopperInstance {
    attributes: Attributes;
    styles: Record<string, Styles>;
}
declare const usePopper: (referenceElement?: Element | VirtualElement, popperElement?: HTMLElement, options?: Options) => InnerPopperInstance;
export default usePopper;
