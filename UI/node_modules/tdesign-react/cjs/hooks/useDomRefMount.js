/**
 * tdesign v1.17.1
 * (c) 2026 tdesign
 * @license MIT
 */

'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');

function useDomRefMount(ref) {
  var callbacks = React.useRef([]);
  var unmountCallbacks = React.useRef([]);
  var onMount = React.useCallback(function (nodeOrCallback) {
    if (typeof nodeOrCallback === "function") {
      callbacks.current.push(nodeOrCallback);
      return;
    }
    var node = nodeOrCallback;
    var prevNode = ref === null || ref === void 0 ? void 0 : ref.current;
    if (ref) {
      ref.current = node;
    }
    if (node && !prevNode) {
      callbacks.current.forEach(function (callback) {
        callback(node);
      });
    }
    if (!node && prevNode) {
      unmountCallbacks.current.forEach(function (callback) {
        callback();
      });
    }
    return node;
  }, []);
  var onUnmount = React.useCallback(function (callback) {
    unmountCallbacks.current.push(callback);
  }, []);
  var clearCallbacks = React.useCallback(function () {
    callbacks.current = [];
    unmountCallbacks.current = [];
  }, []);
  return {
    onMount: onMount,
    onUnmount: onUnmount,
    clearCallbacks: clearCallbacks
  };
}

exports["default"] = useDomRefMount;
//# sourceMappingURL=useDomRefMount.js.map
