import React from 'react';
import type { QRCodeConfig } from '../config-provider/type';
import type { StatusRenderInfo, TdQRCodeProps } from './type';
export type QRcodeStatusProps = {
    locale: QRCodeConfig;
    classPrefix: string;
    onRefresh?: TdQRCodeProps['onRefresh'];
    statusRender?: TdQRCodeProps['statusRender'];
    status: StatusRenderInfo['status'];
};
export default function QRcodeStatus({ locale, classPrefix, onRefresh, statusRender, status }: QRcodeStatusProps): React.JSX.Element;
